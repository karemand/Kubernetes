apiVersion: v1
kind: Service
metadata: 
  name: svc
  namespace: move
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: MyApp
  ports:
     - protocol: TCP
       port: 9090
       targetPort: 90
       nodeport: 30009
